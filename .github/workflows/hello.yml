name: My First Workflow 

on: [push, pull_request]

jobs:
  Say-hello:
    runs-on: ubuntu-latest
    needs: Say-bye
    steps:
       - name: Print message
         run: echo "Hello, Github Actions!"

       - name: Print my name
         run: echo "Gaurish"
  Say-bye:
    runs-on: ubuntu-latest

    steps:
       - name: Print message
         run: echo "Hello, Github Actions!"
         

